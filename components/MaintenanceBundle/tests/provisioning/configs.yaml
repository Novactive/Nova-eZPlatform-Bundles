nova_ezmaintenance:
    system:
        default:
            enable: false
            template: '@@NovaeZMaintenance/maintenance.html.twig'

        site:
            enable: true #Enable maintenance for site access 'site'
            lock_file_id: 'site.lock' #file name for maintenance.lock file
