{% if not ibexa_field_is_empty(content, identifier) %}
    {{ ibexa_render_field(content,identifier,  {
        parameters: {
            'alias': alias,
            'class': classes|join(' '),
            'alt' : imageAssetAlternativeText
        }
    } ) }}
{% endif %}