{% set image_field_identifier = ez_image_asset_content_field_identifier() %}

{% if image_field_identifier is not null and not(ez_is_field_empty(content,  image_field_identifier)) %}
    {{ ez_render_field( content, image_field_identifier, {
        'template': '@EzRssFeed/default/content/fields/fields.html.twig',
    }) }}
{% endif %}