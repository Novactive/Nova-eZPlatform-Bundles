{% trans_default_domain "ezmailing" %}
{% extends pagelayout %}

{% block content %}
    {% if form is defined %}
        {{ form_start(form) }}
        {{ form_row(form.user) }}
        {{ form_row(form.mailingLists) }}
        <input type="submit" value="Register" name="Register"/>
        {{ form_end(form) }}
    {% else %}
        <p>{{ "registration.register.confirmation_email_message"|trans }}</p>
    {% endif %}
{% endblock %}
